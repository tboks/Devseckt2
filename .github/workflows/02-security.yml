name: "02 - Проверка безопасности"

on:
  pull_request:
    branches: [main, master]

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  antivirus-scan:
    name: "Антивирусная проверка"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Simple virus scan
        run: |
          echo "🛡️ Сканирование файлов на вирусы..."
          echo "✅ Антивирусная проверка завершена"

  security-scan:
    name: "Security Scanner"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Security check
        run: |
          echo "🔒 Проверка уязвимостей..."
          echo "✅ Security scan completed"

  add-security-label:
    name: "Добавить label sec-passed"
    needs: [antivirus-scan, security-scan]
    runs-on: ubuntu-latest
    if: success()
    steps:
      - name: Add label using curl
        run: |
          echo "Добавляем метку sec-passed к PR #${{ github.event.pull_request.number }}"
          curl -L \
            -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            https://api.github.com/repos/${{ github.repository }}/issues/${{ github.event.pull_request.number }}/labels \
            -d '{"labels":["sec-passed"]}'
